# 操作手册

> 仿真环境地址：`10.0.0.60`

## 1. 环境信息

| 项目 | 值 |
|------|----|
| 仿真服务器 IP | `10.0.0.60` |
| ROS2 发行版 | Jazzy |
| DDS 实现 | Fast DDS (rmw_fastrtps_cpp) |
| 仿真默认 Domain ID | `42` |
| 默认地图 | `maps/auto_exploration_map.yaml` |
| 项目路径 | `/home/hello/lidar-slam` |

---

## 2. 启动两辆仿真车

本项目约定 Discovery Server 由 openTCS sidecar 端提供。下面示例假设 sidecar 的 Discovery Server endpoint 为 `10.0.0.187:11811`，两辆仿真车都只作为客户端连接该 endpoint。

### 终端 1：启动第一辆车（gazebo_1）

```bash
ssh hello@10.0.0.60
cd /home/hello/lidar-slam
./scripts/launch/dispatch.sh \
  --namespace gazebo_1 \
  --discovery-address 10.0.0.187 \
  --discovery-port 11811 \
  --no-rviz
```

**等待 Gazebo 完全加载**（日志出现 `SpawnEntity` 且不再滚动）后，再启动第二辆车。

### 终端 2：启动第二辆车（gazebo_2）

Gazebo 双车仿真复用同一个 Gazebo 进程，因此两辆车需要连接 sidecar 端同一个 Discovery Server。第二辆车仍需使用 `--skip-cleanup --no-gazebo`，避免清理或重复启动第一辆车已经拉起的 Gazebo。

```bash
ssh hello@10.0.0.60
cd /home/hello/lidar-slam
./scripts/launch/dispatch.sh \
  --namespace gazebo_2 \
  --skip-cleanup \
  --no-gazebo \
  --discovery-address 10.0.0.187 \
  --discovery-port 11811 \
  --no-rviz
```

### 参数说明

| 参数 | 作用 |
|------|------|
| `--namespace gazebo_1/gazebo_2` | 为每辆车分配独立 ROS2 namespace，话题分别为 `/gazebo_1/...` 和 `/gazebo_2/...` |
| `--skip-cleanup` | 第二辆车启动前不清理已有 ROS2/Gazebo 进程，避免把第一辆车停掉 |
| `--no-gazebo` | 第二辆车复用第一辆车已启动的 Gazebo 进程 |
| `--discovery-address` | sidecar 端 Discovery Server 的 IP 或 endpoint |
| `--discovery-port` | sidecar 端 Discovery Server 端口，默认 `11811` |
| `--no-rviz` | 不为每辆车单独启动 RViz（后面统一启动） |

### 默认位姿

| 车辆 | X | Y | Z |
|------|---|---|---|
| `gazebo_1` | `0` | `0` | `0.24` |
| `gazebo_2` | `0` | `2` | `0.24` |

可通过 `--spawn-x`、`--spawn-y`、`--spawn-z` 覆盖。

---

## 3. 可视化双车

另开终端启动多车 RViz：

```bash
ssh hello@10.0.0.60
cd /home/hello/lidar-slam
./scripts/launch/rviz_multi.sh --discovery-address 10.0.0.187
```

如果 RViz 也接入 sidecar 提供的 Discovery Server，`--discovery-address`
应填写 sidecar 的 Discovery Server 地址，例如 `10.0.0.187`。

---

## 4. 验证

在 Fast DDS Discovery Server 模式下，`ros2 topic list` / `ros2 action list`
有时只能看到节点或部分 graph 信息。判断链路是否 ready 时，优先使用明确类型的
`ros2 topic echo` 或 action client 探测。

```bash
ros2 node list | grep -E "/gazebo_1|/gazebo_2"
```

可能输出：

```text
/gazebo_1/opentcs_vehicle_node
/gazebo_2/opentcs_vehicle_node
```

关键 topic 首帧验证：

```bash
ros2 topic echo /gazebo_1/joint_states sensor_msgs/msg/JointState --once
ros2 topic echo /gazebo_2/joint_states sensor_msgs/msg/JointState --once
ros2 topic echo /gazebo_1/amcl_pose geometry_msgs/msg/PoseWithCovarianceStamped --once
ros2 topic echo /gazebo_2/amcl_pose geometry_msgs/msg/PoseWithCovarianceStamped --once
ros2 topic echo /gazebo_1/robot_state std_msgs/msg/String --once
ros2 topic echo /gazebo_2/robot_state std_msgs/msg/String --once
```

`/gazebo_1/amcl_pose` 和 `/gazebo_2/amcl_pose` 是给 sidecar / RViz 使用的定位输出。
AMCL 原始输出在车端内部 remap 为 `amcl_pose_raw`，通常不需要 sidecar 订阅。
车辆等待调度时，`robot_state` 中应能看到 `state=IDLE`、`localizationStatus=OK`，
且 `currentTransportOrder` / `goalId` 为空。

手动下发导航目标：

```bash
ros2 topic pub --once /gazebo_1/goal_pose geometry_msgs/msg/PoseStamped \
  "{header: {frame_id: 'map/orderId=G1-TEST'}, pose: {position: {x: 5.0, y: 0.0, z: 0.0}, orientation: {w: 1.0}}}"

ros2 topic pub --once /gazebo_2/goal_pose geometry_msgs/msg/PoseStamped \
  "{header: {frame_id: 'map/orderId=G2-TEST'}, pose: {position: {x: 5.0, y: 2.0, z: 0.0}, orientation: {w: 1.0}}}"
```

物料 action 完整名称如下，sidecar 不应只使用 `/gazebo_1` 或 `/gazebo_2`
作为 action name：

```text
/gazebo_1/load_materials
/gazebo_1/unload_materials
/gazebo_2/load_materials
/gazebo_2/unload_materials
```

---

## 5. Sidecar 端环境

sidecar 端 Discovery Server 与 sidecar ROS2 进程使用同一个 endpoint。sidecar ROS2 进程启动前设置：

```bash
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export ROS_DOMAIN_ID=42
export ROS_DISCOVERY_SERVER="10.0.0.187:11811"
unset ROS_LOCALHOST_ONLY
```

验证：

```bash
ros2 topic echo /gazebo_1/robot_state std_msgs/msg/String --once
ros2 topic echo /gazebo_2/robot_state std_msgs/msg/String --once
```

---

## 6. 两台实车连接 Sidecar Discovery Server

假设：

| 项目 | 值 |
|------|----|
| Sidecar Discovery Server | `192.168.10.10:11811` |
| 车 1 Namespace | `c30_1` |
| 车 2 Namespace | `c30_2` |

车 1：

```bash
./scripts/launch/dispatch.sh \
  --profile raspberry \
  --namespace c30_1 \
  --discovery-address 192.168.10.10 \
  --discovery-port 11811 \
  --no-rviz
```

车 2：

```bash
./scripts/launch/dispatch.sh \
  --profile raspberry \
  --namespace c30_2 \
  --discovery-address 192.168.10.10 \
  --discovery-port 11811 \
  --no-rviz
```

Sidecar 端：

```bash
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export ROS_DOMAIN_ID=30
export ROS_DISCOVERY_SERVER="192.168.10.10:11811"
unset ROS_LOCALHOST_ONLY
```

---

## 7. 注意事项

1. Discovery Server 由 sidecar 端提供，车辆端只连接，不启动本地 Discovery Server
2. Gazebo 双车仿真使用同一个 Gazebo 进程，必须让两辆车连接同一个 sidecar Discovery Server；否则第二辆车的 Gazebo 控制插件看不到 `/gazebo_2/robot_description`
3. 所有节点必须在同一个 `ROS_DOMAIN_ID` 下；仿真默认 `42`，实车默认 `30`
4. 必须等第一辆车的 Gazebo 完全加载后，再启动第二辆车
5. 确保车辆、仿真机与 sidecar 之间网络可达，端口 `11811` 未被防火墙拦截
